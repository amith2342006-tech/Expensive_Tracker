# models/

This project stores data in Firestore (see the firestore.rules file at the repo root),
so there are no traditional ORM models here. If you prefer a SQL or Mongo
database instead of Firestore, this is the folder to add, for example, Mongoose schemas
or Prisma models for Expense, Income, Budget, and Goal.
